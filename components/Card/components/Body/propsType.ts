export interface CardBodyProps {
    prefixCls?: string,
    className?: string,
    borderDirection?: string,
    children: any,
    noBorder?: boolean,
    full?: boolean,
    style?: React.CSSProperties,
}